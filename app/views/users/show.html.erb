<div class="row">
  <div class="col-xs-4">
  <h3 class="form-group">ユーザー情報</h3>
    <div class="table-responsive">
      <table class="table">
        <tr>
          <td>ユーザー名</td>
          <td><%= @user.name %></td>
        </tr>
        <tr class="text-center">
          <td colspan="2"><%= attachment_image_tag @user, :profile_image, format: "jpeg", fallback: "l_e_others_476.jpg", size: "100x100" %></td>
        </tr>
        <tr>
          <td>地域</td>
          <td><%= @user.address %></td>
        </tr>
        <tr>
          <td colspan="2">紹介文<br>
          <% if @user.introduction.present? %>
            <%= safe_join(@user.introduction.split("\n"), tag(:br)) %>
          <% end %></td>
        </tr>
        <% if @user == current_user %>
          <tr> 
            <td colspan="2"><%= link_to "編集", edit_user_path, class: "btn btn-default  center-block" %></td>
          </tr>
        <% end %>
      </table>
    </div>
  </div>

  <div class="col-xs-8">
    <h3>記事一覧</h3>
      <div class="table-responsive">
        <table class="table">
          <thead>
            <th></th>
            <th>タイトル</th>
            <th>ユーザー名</th>
          </thead>
          <tbody>
            <% @articles.each do |article| %>
              <tr>
                <td><p><%= attachment_image_tag article, :article_image, format: "jpeg", size: "80x80", follback: "l_e_others_470.jpg" %></p></td>
                <td><p><%= link_to "#{article.title.truncate(30)}", article_path(article) %><p></td>
                <td><p><%= article.user.name %></p></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
  </div>
</div>